// imports required libraries and files
import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css";

// uses the reactdom library to render the entire app in the container designated by the document.getElementById call.
ReactDOM.render(<App />, document.getElementById("root"));
